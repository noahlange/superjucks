{
  "name": "superjucks",
  "description": "Nunjucks-inspired templating language from the future!",
  "version": "0.2.0",
  "nyc": {
    "reporter": [
      "html",
      "lcov"
    ],
    "exclude": [
      "lib/tests"
    ]
  },
  "scripts": {
    "build": "tsc",
    "test": "tslint --project tslint.json && nyc ava -v lib/tests/**/*.js lib/tests/*.js"
  }
}
